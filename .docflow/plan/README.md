# Work queue

Use `todo/NNNN-slug.md` for authorized implementation, lowest number first.
Each item records its number, owning decisions, scope, dependencies and exit
criteria. Use `done/YYYY-MM-DD-slug.md` after implementation and validation.
The completion footer names the implementation tip before the completion commit;
the successful push is the completion event. See [conventions](../CONVENTIONS.md).
